< infile cat < infile2

<script>
<simple command>
<redirection> <simple-command>
< <word> <simple-command>
< (STRING) <simple-command>
< infile <simple-command>
< infile <simple-command> <redirection>
< infile <word> <redirection>
< infile (STRING) <redirection>
< infile cat <redirection>
< infile cat < <word>
< infile cat < (STRING)
< infile cat < infile2



export ARG=argument

<script>
<simple-command>
<word> <word>
(STRING) (STRING)
(STRING) (STRING)
(STRING) (STRING)
(STRING) (STRING)
(STRING) (STRING)
(STRING) (STRING)
(STRING) (STRING)

(ls && cat outfile) || echo $USER
<script>
<logical expression> <logical expression>
(<simple-command> <logical-symbol> <simple-command>) <logical expression>
(<word> <logical-symbol> <simple-command>) <logical expression>
((STRING) <logical-symbol> <simple-command>) <logical expression>
((STRING) && <simple-command>) <logical expression>
((STRING) && <word> <word>) <logical expression>
((STRING) && (STRING) <word>) <logical expression>
((STRING) && (STRING) (STRING)) <logical expression>
((STRING) && (STRING) (STRING)) || echo $USER
